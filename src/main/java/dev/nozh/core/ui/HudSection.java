package dev.nozh.core.ui;

/**
 * HUD section enum (Contract 7).
 * 
 * Defines the 7 navigable sections of the NOZH HUD.
 */
public enum HudSection {
    DASHBOARD,
    TUNING,
    PROVIDERS,
    HISTORY,
    BENCHMARK,
    ISSUES,
    CHAOS
}
